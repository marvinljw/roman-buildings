version: '3.8'

services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: roman_history_map
      POSTGRES_USER: roman_user
      POSTGRES_PASSWORD: secure_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: