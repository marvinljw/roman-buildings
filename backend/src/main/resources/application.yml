spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/roman_history_map
    username: roman_user
    password: secure_password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect